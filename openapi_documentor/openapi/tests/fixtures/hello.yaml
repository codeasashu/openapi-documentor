openapi: 3.0.0
info:
  title: Minimal API
  version: 1.0.0
paths:
  /hello:
    get:
      summary: Retrieve a greeting message
      responses:
        "200":
          description: Successful response
          content:
            text/plain:
              schema:
                type: string
